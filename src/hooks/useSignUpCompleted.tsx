const useSignUpCompleted = (userId: string) => {
  // todo: search through the database and check if the user's id has a role
  // ? role: either 'Rescuer' or 'Victim'
  // ?: if does not: let him choose
  // ?: else: return and continue
  return;
};

export default useSignUpCompleted;
